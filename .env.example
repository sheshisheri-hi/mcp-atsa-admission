# No secrets. This gate is CPU-only and reads clearance / policy JSON from disk.
# Optional CLI-equivalent knobs if you wrap it in a host (not read by the package today):
# MCP_ATSA_TRUST_ROOT=fixtures/trust_root.json
# MCP_ATSA_POLICY=fixtures/policy.json
# MCP_ATSA_ALLOW_LIST=fixtures/allow_list.json
# MCP_ATSA_AUDIT=audit.jsonl
